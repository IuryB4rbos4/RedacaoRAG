services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    volumes:
      - ./backend/app:/app/app
      - ./embeddings-db:/app/embeddings-db
      - ./knowledge_base.json:/app/knowledge_base.json
    ports:
      - "8000:8000"
